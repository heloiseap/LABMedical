import { CalculoIdadePipe } from './calculo-idade.pipe';

describe('CalculoIdadePipe', () => {
  it('create an instance', () => {
    const pipe = new CalculoIdadePipe();
    expect(pipe).toBeTruthy();
  });
});
